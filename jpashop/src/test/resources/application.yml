spring:

  # spring은 test 디렉터리 내부에 datasource에 대한 설정이 없으면 디폴트를 메모리 모드로 설정함.
  # 따라서 자동으로 메모리를 통해 테스트를 하기 때문에 굳이 아래의 코드를 작성해 줄 필요가 없음...

#  datasource:
#    url: jdbc:h2:mem:test # h2 db가 아닌 메모리 모드로 테스트 실행 가능
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # jsp나 하이버네이트의 모든 로그를 로거를 통해 출력
    org.hibernate.type: trace


